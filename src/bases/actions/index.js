import { onCheckBoxAll, onCheckboxItem, findIndex } from "./checkboxTable";
import { formatNumber } from "./formatNumber";

export {
    onCheckBoxAll, onCheckboxItem, findIndex,    //checkbox on table
    formatNumber, //find index in a array
}